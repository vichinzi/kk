Para compilar la tarea usar:

make main

Para ejecutar la tarea usar: 

./exe


NOTA:

La tarea funciona solo para palabras de largo menor o igual a 20, supimos resolver el problema correctamente pero nos tomo por sorpresa el hecho de que en C no se puedan almacenar enteros mas grandes que el 20! y lamentablemente nos dimos cuenta de eso tarde como para arreglarlo. 